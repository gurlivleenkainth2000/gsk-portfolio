/**
 * Single point of import for all icons.
 *
 * - Custom SVGs: `logo`, `brand` (social), `ui` (theme / search / heart).
 * - Material icons: re-exported from `material` (see that file for the
 *   tree-shaking convention before adding new ones).
 *
 * Usage: `import { GithubIcon, WorkIcon } from "@/components/icons";`
 */
export * from "./logo";
export * from "./brand";
export * from "./ui";
export * from "./material";
